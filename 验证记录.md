# 餐饮与攻略功能验证

## 自动验证

- `npm run check`：餐饮约束 / Provider / 原有酒店与优化器单元测试、生产构建（含 TypeScript）及生产 HTTP 冒烟。
- `npm run test:smoke`：需要先构建；启动隔离端口的生产服务器，分别运行模拟 Provider 和断网/无密钥两种场景。测试进程使用专用 preload 拦截外部网络，正式应用不包含该测试开关。
- `npm run test:browser`：需要先构建及提供 Playwright。可设置 `PLAYWRIGHT_MODULE_PATH` 指向本机 Playwright 的 `index.mjs`；使用已安装 Edge 时设置 `PLAYWRIGHT_CHANNEL=msedge`，否则使用已安装的 Playwright Chromium。
- 浏览器验证覆盖桌面 1440px 和手机 390px，生成、换店、费用更新、锁定/解锁、公开来源与降级页面，断言没有客户端异常与手机横向溢出。
- 截图写入被 Git 忽略的 `test-artifacts/`，测试不提交密钥或真实外部数据。

## 可复现手动验证

1. `npm run dev`，填写杭州、1 天、全员 6000 元、2 人、杭帮菜、绕路 30 分钟，生成方案。
2. 展开门店卡片，检查具体分店、地址、价格区间与全员费用，查看逐段路线、查询时间和原路线差值。
3. 点击“更省钱”，确认选中门店与费用更新；点击“锁定这家”，确认换店按钮禁用；解锁后“更顺路”，确认额外分钟数下降。没有更优解时应显示原因。
4. 将预算改为最低值或绕路改为 0，检查不符合条件的候选不自动入选；填写花生过敏，检查缺少门店确认时不宣称安全。
5. 展开帖子补充区，粘贴包含准确分店名/景点名的正文，可选填原文链接和发布日期。检查 Tips 引用来自正文，只有品牌名时不冒认具体分店。页面不自动抓取用户链接。
6. 未配置 Tavily 时应显示自动检索未配置；未配置高德时应显示餐次待安排。已有行程、预算、酒店建议仍可查看。
7. 在手机宽度查看全部卡片，确认无横向滚动、按钮可点击、来源链接与长地址换行正常。

## 本次执行情况（2026-09-06）

- 完成约束及 Provider 测试、生产构建和两类生产 HTTP 冒烟。
- Edge 无头浏览器完成模拟 Provider 和无密钥两类流程，桌面/手机截图已人工查看；无页面脚本异常或横向溢出。
- 使用现有环境配置执行杭州真实查询，高德具体门店、DeepSeek 与天气查询成功。自动小红书检索尚未进行真实联调：当前缺少 `TAVILY_API_KEY`，接口契约和失败降级已通过模拟测试。
- 价格、营业时间与未来交通只能作为参考；未通过约束的餐厅保持未安排，不把待确认信息伪装为预算/时间保证。
- 环境中的既有密钥未改动；`.env` 与 `.env.local` 均不会进入本次提交。新配置仅在 `.env.example` 增加空值 `TAVILY_API_KEY`。

## 已知范围

自动检索覆盖公开搜索收录范围，不能保证搜到所有小红书帖子；榜单线索不等于官方榜单认证。原有景点行程仍是距离排序的候选日程，完整每日交通/预约校验尚未实现；新餐饮模块只对用餐插入的相关路段和用餐时段执行校验。换店保留景点顺序和其他餐次，方案内存暂存 30 分钟，服务重启后需重新生成。

## 多日及美食回归（2026-09-07）

- 重现根因：原实现仅使用 4 个候选，第二天后反复倒序切同一数组；餐厅仅检索前站的少量 POI，未通过全部条件时所有店名都折叠隐藏。
- 回归覆盖 1/2/3/5/10 天、模型输出超出 4 项、POI 别名、候选不足、跨年日期、每日停留与交通间隔；候选不足时不重复填充，未知离线城市不借用杭州地点。
- 扩大餐饮搜索后，测试覆盖后站附近门店、前 8 条全部超预算而后续有可用餐厅、奶茶/咖啡不得替代正餐。
- 生产 HTTP 冒烟加入三日请求：9 个不同地点、6 个餐次均有具体候选；无密钥场景明确候选不足。浏览器加入三日 + 花生过敏场景，确保所有餐次均在不展开折叠区的情况下显示具体待确认店名，同时不自动宣称符合忌口。
- 使用现有配置的杭州三日真实查询已返回 9 个不同景点、6 个餐次的具体门店，以及 5 篇公开小红书笔记。仅查询了当前数据，不保证相同查询将来返回相同结果。现有 Tavily 配置已可检索，此项更新前一日“未完成真实联调”的记录。
- 手动复现：杭州、3 天、全员 6000 元、2 人、杭帮菜/面食、额外交通 30 分钟、用餐 60 分钟、排队 15 分钟；检查每天景点不同、每餐有具体分店名。然后填入饮食禁忌，检查待确认店名直接展示、未满足条件的店不计入已安排预算。
- 截图：`test-artifacts/multiday-food-mobile.png` 与 `test-artifacts/multiday-day-desktop.png`；测试产物不提交。
- 最终生产冒烟额外模拟 AI 故障，验证高德补充后仍有 9 个不同地点。真实餐饮复测已排除饮品店，6 个餐次均有具体候选，其中 5 餐满足筛选自动入选、1 餐保留待确认；示例包含翠薇面斋、灵隐大食堂、赵府私厨等，结果仅对应本次查询。

## 多目的地、顺路排序与交互地图（2026-09-07）

### 自动验证

- `npm run check`：37 项单元测试全部通过；Next.js 16.3.4 生产构建、TypeScript 检查通过；模拟 Provider 与无密钥两类生产 HTTP 冒烟通过。
- 新增双目的地冒烟：从上海出发选择北京、杭州，模拟高德坐标后按杭州 → 北京排列；验证每个目的地至少一天、城市顺序与日程一致、出发地到各城市有 2 段衔接路线、地图点序号连续。
- 新增路线点单元测试：出发地、景点、已选餐厅按访问时间组成连续序号；未定位地点和未选餐厅不伪装成地图标记。
- `npm run test:browser`：使用 Playwright 1.55.1 与 Windows Edge，模拟 Provider、无密钥两种模式均通过；验证省市下拉搜索、默认选择、路线图显示、地点详情切换、按天筛选、桌面与 390px 手机无横向溢出、无页面脚本错误。
- `npm audit --omit=optional`：0 个已知漏洞。Playwright 初次补入的 1.55.0 存在已公开下载校验证书问题，已升级到 1.55.1 后复查通过。

### 人工检查

- 查看 `fixtures-desktop.png`、`fixtures-mobile.png`、`offline-desktop.png`、`offline-mobile.png`：出发地与目的地多选布局清晰；地图序号、虚线访问顺序、缩放/日期按钮和横向地点条可见；详情浮层不会拦截地图点击；结果页在桌面和手机布局正常。
- 路线图明确显示 Provider 来源与查询时间；无高德时显示顺序示意和待确认说明，不把直线连线冒充实时道路。

## 候选勾选、公开证据与导航升级（2026-09-07）

### 验证范围

- 单元测试新增候选服务端存储的过期、防伪 ID、跨城市最低选择约束；高德导航参数及境外坐标拒绝；图片代理白名单、仿冒域名、凭据、端口拦截；娱乐 POI 类别、图片代理地址与导航生成。
- 生产 HTTP 冒烟改为完整两阶段链路：发现三类候选、检查小红书逐字证据/高德图片代理/导航链接，提交服务端候选 ID 后生成路线；伪造 ID 返回冲突。无密钥模式不得产生伪候选，并显示明确降级提示。
- 浏览器验证覆盖候选图片、三类卡片勾选、选择门槛、生成路线、每站高德导航、路线序号、餐厅换店/锁定，以及桌面和 390px 手机布局；截图继续写入 `test-artifacts/`。
- 手动复现：填写杭州 1 天、全员 6000 元、2 人和杭帮菜，点击“发现景区、美食与娱乐候选”；勾选至少一个景区或娱乐，可选一家有公开笔记证据的餐厅，再生成路线。检查每站导航 URL 指向 `uri.amap.com`，证据链接/发布日期/查询时间可见。

### 本次最终结果

- `npm run check`：41 项单元测试全部通过；Next.js 16.3.4 生产构建和 TypeScript 检查通过；模拟 Provider与无密钥两类生产 HTTP 冒烟通过。
- `npm run test:browser`：Windows Edge 下模拟 Provider 与无密钥两种模式均通过，两阶段选择、导航链接、桌面/手机响应式布局均无客户端异常。
- `npm audit --omit=optional`：0 个已知漏洞；`git diff --check` 无空白错误（仅有 Windows 行尾转换提示）。

真实 Provider 返回具有时效性，本次自动验证使用隔离模拟数据，不将测试密钥或外部响应写入提交。

## Windows 一键启动（2026-09-07）

- 项目根目录新增 `启动 TravelCanvas.cmd`。用户双击后由本机 PowerShell 脚本检查 Node.js、既有依赖和生产构建，启动仅监听 `127.0.0.1:3000` 的 Next.js 服务并打开默认浏览器，无需手工输入命令。
- 已有 TravelCanvas 服务时直接打开页面；端口被其他网页占用时停止并提示，不误把其他页面当成 TravelCanvas。源码或关键配置比 `.next/BUILD_ID` 新时自动执行生产构建。
- 启动器不会自动安装依赖、写入环境文件或处理 API 密钥。首次缺少依赖时会提示用户先执行一次 `npm install`；服务日志保留在独立窗口，关闭该窗口即可停止本地网站。
- `tests/launcher.test.mjs` 静态验证本地绑定、生产启动、浏览器打开、按需构建，以及“不自动安装依赖/不处理密钥”的安全边界；PowerShell 解析器检查用于确认脚本语法有效。

### 后台存活修正

- 原启动器通过 `npm.cmd` 启动一个可见的中间控制台；该控制台退出或被关闭后，浏览器仍保留已加载页面，但 `/api/discover` 已无本地服务可连接，因此显示浏览器原始错误 `Failed to fetch`。
- 新启动器直接以 `node.exe` 运行 Next.js CLI，并使用独立隐藏后台进程；PID 和标准输出/错误日志保存到 `%LOCALAPPDATA%\TravelCanvas`。新增 `停止 TravelCanvas.cmd`，仅在 PID、Node 可执行文件和项目 Next.js 命令行都匹配时结束进程，避免误杀 PID 被复用后的其他程序。
- 前端将 Fetch/Network 类型的连接失败转换为中文恢复指引，服务端返回的业务错误仍原样展示。自动测试覆盖错误分类、后台进程、PID、日志和安全停止逻辑。
- 最终验证：PowerShell 启动/停止脚本语法检查通过；独立端口实际启动后启动脚本退出、后台 Node 进程仍存活且 HTTP 返回 200，停止脚本可安全结束该进程；47 项单元测试、生产构建、两类 HTTP 冒烟和桌面/手机浏览器测试全部通过。
- 兼容旧启动器遗留的服务：若本地页面已运行但没有 PID 文件，新启动器会读取监听端口的进程，并在确认其为当前项目的 Node/Next.js 命令后记录 PID，使“一键停止”同样可用；其他项目或非 Node 进程不会被接管。

## 真实高德路线、餐厅确认与逐日分页（2026-09-07）

- 单元测试扩展到 53 项：验证 Tavily Advanced 三查询及精准分店追查/去重、高德 POI 详情补查价格/营业时间、路线规划 2.0 字符串与嵌套对象折线解析、用户已选但信息未知的餐厅可安排，以及明确安全/营业冲突仍不可覆盖。
- `npm run check` 覆盖全部单元测试、Next.js 16.3.4 生产构建/TypeScript 和模拟 Provider/离线两类生产 HTTP 冒烟；冒烟额外检查每天都有天气/酒店/提醒数据，且路线段返回实时状态。
- `npm run test:browser` 使用 Windows Edge 完成桌面 1440px 与手机 390px 回归：候选三类勾选、行程生成、真实地图无 Key 降级提示、地点详情、每日分页、餐厅换店/锁定和响应式页面均无客户端异常。
- 人工查看 `test-artifacts/fixtures-desktop.png` 与 `fixtures-mobile.png`：候选娱乐数量增加，地图卡片与地点详情可见，每日天气/住宿/提醒位于当天行程前；移动端无横向溢出。
- 当前本机环境尚未配置 `NEXT_PUBLIC_AMAP_JS_KEY` 与 `NEXT_PUBLIC_AMAP_SECURITY_JS_CODE`，因此本次浏览器验证覆盖了安全降级，而非真实在线底图。配置受域名限制的专用 JS Key 后，代码会加载高德底图、服务端道路折线和编号 Marker；不能用现有服务端 `AMAP_API_KEY` 代替。
- 真实价格、节假日营业变化、临时停业及未来交通仍无法保证完整；系统会用 POI ID 补查详情、解析常见分时/跨夜营业格式、重试路线请求，并将剩余未知项保留为可接受的“待确认”，不会补造事实。
- 使用本机现有 Provider 配置进行杭州真实链路验证：AI、Tavily 与高德候选查询均返回 live；生成 22 个候选（6 景区、6 餐厅、10 娱乐），用户勾选餐厅进入餐次，3 条可用市内路线均返回高德真实折线（单段抽查 435 个坐标点），并生成 1 份逐日指南。该次公开笔记仍未形成能绑定到具体分店的逐字证据，因此页面保持“无证据”而未虚构招牌菜；后续精准分店检索是否命中取决于公开搜索收录。

## 自定义地点与按天统一重排（2026-09-07）

- `npm run check`：58 项单元测试全部通过；Next.js 16.3.4 生产构建及 TypeScript 检查通过；模拟 Provider 与完全离线两类生产 HTTP 冒烟通过。
- 冒烟验证：自定义景区/饭店接口只追加高德核验候选；娱乐候选不在第一阶段出现，路线生成后按每天末站筛选；景区替换和删除娱乐活动经版本校验后重建当天路线；餐次不会被夜间娱乐挤掉。
- `npm run test:browser`：Windows Edge 下模拟 Provider和离线两种模式通过。浏览器实际完成批量输入两个景点、自动加入候选、景区/餐厅选择、行程生成、娱乐下拉删除及统一保存，并验证预约提示、每日携程链接、右侧天气/酒店去重、地图交互及 390px 移动端无横向溢出。
- 人工查看 `test-artifacts/fixtures-desktop.png` 与 `fixtures-mobile.png`：批量输入区、每日天气/酒店/提醒、景区预约提示、景点替换输入、娱乐下拉和统一保存按钮均在对应层级显示；桌面与手机布局未见遮挡。
- 预约解析测试只接受准确 `.gov.cn` HTTPS 域名、准确景区名和明确预约措辞；仿冒域名、非政府攻略和未匹配内容保持“预约要求待确认”。自动验证使用隔离模拟数据，没有消耗真实 Provider 查询额度，也没有记录密钥。

## 联网补图、动态预算与分类娱乐检索（2026-09-07）

- 61 项单元测试全部通过；新增覆盖高德缺图时 Tavily 图片查询、不透明服务端代理、原图短时存储、危险 URL 拒绝，以及步行/公交/驾车预算比例和一日游不预留住宿。
- Next.js 16.3.4 生产构建与 TypeScript 检查通过；新增 `/api/plan/entertainment` 与 `/api/web-image` 均进入生产路由表。
- 模拟 Provider 与完全离线两类生产 HTTP 冒烟通过：基础方案不再自动混入台球；以“足浴”独立检索后可选择具体地点加入当天行程，再删除并替换景点。
- Windows Edge 浏览器回归通过：台球、足浴、剧本杀、酒馆均出现在类型选择框；实测选择足浴、按当天路线查找、加入草稿并统一保存后，行程增加足浴地点。桌面 1440px 和手机 390px 均无横向溢出或页面脚本错误。
- 人工查看 `test-artifacts/fixtures-desktop.png`：娱乐类型、补充地点输入、具体地点选择、草稿删除和统一保存处于同一编辑区；预算卡区分建议上限、已知路线交通估算和未分配余额。测试使用隔离模拟数据，未消耗真实 Provider 额度。

## 娱乐选择去重与候选扩容（2026-09-08）

- 删除旅行需求阶段的娱乐多选，避免用户先选一次、生成路线后再选一次；娱乐类型统一在每天行程中决定。
- “其他”切换为明确的必填活动类型输入，浏览器测试验证未填写时不能查询、填写“密室逃脱”后可查询；HTTP 冒烟验证服务端返回标记为“其他：密室逃脱”的具体地点。
- 高德检索组合城市、当天末站、活动类型和补充区域等关键词，候选池扩大到 30 个，再以 45 分钟顺路范围筛选最多 12 个。冒烟与浏览器测试均验证候选数量超过旧版 6 个上限。
- `npm run check` 通过：61 项单元测试、Next.js 16.3.4 生产构建、TypeScript 和模拟/离线 HTTP 冒烟均通过；`npm run test:browser` 在 Windows Edge 的桌面与 390px 手机视口通过。人工查看 `fixtures-desktop.png`，需求表单不再出现重复娱乐勾选，每日娱乐编辑区布局正常。

## 娱乐地点 15 公里筛选（2026-09-08）

- 娱乐候选不再按 45 分钟交通时间淘汰，改为使用高德路线接口返回的道路距离筛选当天末站 15 公里以内地点，并按距离、交通时间排序；页面同时显示公里数和参考分钟数。
- HTTP 冒烟断言所有返回娱乐候选都有有效路线距离且不超过 15000 米；Windows Edge 浏览器回归验证候选下拉框显示“距离 0.6 公里”。
- `npm run check` 通过：61 项单元测试、生产构建、TypeScript 和模拟/离线 HTTP 冒烟全部成功；`npm run test:browser` 的桌面和 390px 手机验证通过。

## 偏好筛选与餐厅软约束（2026-09-08）

- 新增确定性的景点偏好二次筛选：识别老年人、长辈、行动不便、少走路等需求，优先文化场馆、公园、园林等低强度地点，并排除攀岩、漂流、高空等明显冲突项目；候选卡显示匹配理由和限制提醒。
- 美食偏好扩展为可检索的同义词并直接参与高德关键词查询、候选排序和结果解释，避免不同偏好只影响 AI 文案而不改变实际门店池。
- 未知餐价不再折算为 0 元；预算摘要显示“价格待确认（不是 ¥0）”及未知餐次数量。预算、绕路、营业时间等软冲突可在明确提示后由用户选择并锁定，饮食禁忌的明确冲突仍不可强制覆盖。
- `npm run check` 通过：66 项单元测试、Next.js 16.3.4 生产构建、TypeScript，以及模拟 Provider/完全离线两类 HTTP 冒烟全部成功。新增老年人偏好集成断言、偏好关键词 Provider 断言、未知价格汇总和软冲突锁定测试。
- `npm run test:browser` 在 Windows Edge 下通过模拟 Provider 与离线两种模式；实际验证未知价格提示、杭帮菜匹配解释、超预算餐厅提示后锁定、预算超出汇总，以及桌面和 390px 手机布局。人工检查 `fixtures-desktop.png`，候选匹配提示和餐厅风险信息层级清晰。

## Impeccable 产品上下文初始化（2026-09-08）

- 新增 `PRODUCT.md`，记录已确认的平台、目标用户、产品目的、两阶段可信规划定位、能力边界、品牌口吻、现有证据和长期产品原则。
- 用户确认目标人群为热爱旅游且具备支付能力的个人旅行者，品牌口吻为“便捷、高效”；视觉风格未写入产品事实，避免限制后续设计探索。
- `.impeccable/config.json` 将后续新界面的默认构建方式记录为用户选择的 `comp-first`；Live 视觉迭代检查确认还需先通过 `$impeccable document` 建立 `DESIGN.md`，本次初始化没有越权生成视觉规范。
- 可复现验证：检查文件包含 `<!-- impeccable:product-schema 1 -->`、`## Platform` 的 `web` 值和所有已确认产品事实；运行 `git diff --check`，无空白错误。此次仅新增设计上下文文档，不改变运行时代码，因此不重复执行应用测试。

## Impeccable 设计系统记录（2026-09-08）

- 新增 `DESIGN.md`，从现有 CSS 与组件提取颜色、字号、间距、圆角、阴影、断点和组件规则；用户确认“城市探索罗盘”为创意北极星，整体气质为精准、高效、专业，颜色命名采用“茶山绿＋陶土橙”，并明确避免企业后台感。
- 新增 `.impeccable/design.json` schema v2 sidecar，包含 11 组颜色元数据与 tonal ramps、6 个文字角色、5 个阴影、2 个响应式断点、8 个可独立预览的代表组件，以及与 `DESIGN.md` 一致的叙事和规则。
- 可复现验证：使用 Impeccable 自带 `design-parser.mjs` 解析 `DESIGN.md`，确认八个规范章节、11 个颜色、5 个字号层级、阴影和 Do/Don't 规则均可识别；使用 Node.js 解析 sidecar 并检查 schema、北极星和组件数量；运行 `git diff --check`。此次只记录现有视觉实现，没有修改运行时页面，因此不重复执行应用测试。

## Impeccable 首页结构探索（2026-09-08）

- 为完整“需求—候选—行程”首页生成三套 1440×1000 高保真结构稿：地图主导的“路线即首页”、快速输入的“一屏启程台”和阶段折叠的“折叠旅程带”。三套方案均继承既有“城市探索罗盘”视觉身份，只比较信息架构。
- 每张 PNG 均配套保存原始 prompt sidecar，并嵌入完整生成提示词；明确使用低细节地图或地点插画作为设计占位，不把生成图当作真实景区图片或产品事实。
- 可复现验证：读取 PNG 文件头确认三张图均为 1440×1000；解析三个 sidecar，检查 `useCase: ui-mockup`、`approved: false` 和非空 prompt；运行 Impeccable `embed-prompt.mjs --scan .impeccable/mocks/decision`，结果为 3 张图片、0 个缺失 provenance；解析选择页 payload，并运行 `git diff --check`。选择页运行状态目录已加入 `.gitignore`。
- 用户在视觉选择页确认“路线即首页”，构建方式保持 `comp-first`；对应 `route-first-canvas.json` 已标记 `approved: true`，另外两套结构仍保持未批准状态。本次 shape 到设计简报为止，不修改页面实现。

## 空目的地、天气、已订酒店与娱乐时段（2026-09-19）

- `npm run check` 通过：76 项单元测试、Next.js 16.3.4 生产构建、TypeScript、模拟 Provider 和完全离线 HTTP 冒烟均成功。
- `npm run test:browser` 在 Windows Edge 的 1440px 桌面和 390px 手机视口通过；覆盖空目的地禁用提交、候选自动收起/重新展开、已订酒店校验及地图图例、三个娱乐时段和保存后的时间重排。
- 单元测试额外覆盖高德天气优先/Open-Meteo 坐标回退、超期不伪造 0 值、酒店入住/中间日/退房/换店锚点、娱乐时段容量冲突及晚餐提前。

## 公开小红书旅游攻略增强（2026-09-21）

- `npm run check` 通过：80 项单元测试、Next.js 16.3.4 生产构建、TypeScript、模拟 Provider 和完全离线 HTTP 冒烟全部成功。覆盖每城一次查询、最多 8 篇、相关性排序去重、公开正文/搜索摘要逐篇降级、伪造引用丢弃及攻略证据增强景点排序。
- `npm run test:browser` 在 Windows Edge 的 1440px 桌面和 390px 手机视口通过；验证了“公开搜索相关性前 8 篇”说明、景点原文证据、生成路线后的攻略来源及桌面/手机无水平溢出。检查 `fixtures-desktop.png` 与 `fixtures-mobile.png`，新增来源区块未破坏布局。
- 可选 Python 辅助脚本在未安装 Scrapling 时输出空 JSON 并正常退出，证明不会阻断主流程；启动器测试仍验证不自动安装任何依赖。
- 限量实时验证：2026-09-21T11:23:41.848Z 使用本机 Tavily 密钥查询“杭州 旅游攻略”，返回 1 篇通过白名单的 `www.xiaohongshu.com` 公开结果。这说明“前 8 篇”是上限而非保证数量，实际收录会随 Tavily 索引变化。本机未配置服务端 `AMAP_API_KEY`，因此未做高德天气实时请求，其 Provider 选择由模拟测试验证。
