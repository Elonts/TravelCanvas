# TravelCanvas 技术方案

## 1. 技术目标与范围

TravelCanvas 面向中国境内自由行用户，基于自然语言需求生成旅行方案，并用真实地图、天气与酒店数据校验和补全方案。

本阶段边界：

- 仅支持中国境内目的地。
- 酒店只提供查询、筛选与供应商预订页跳转，不处理支付、订单或售后。
- 第三方业务 API 由服务端调用。高德 JS API 使用独立、受域名白名单限制的浏览器 Key；服务端 `AMAP_API_KEY` 绝不下发或复用。
- 优先实现“地点可信、路线省时、信息有更新时间”的旅行方案。

## 2. 推荐技术栈

| 层级 | 技术选择 | 用途 |
| --- | --- | --- |
| 全栈框架 | Next.js + React + TypeScript | 页面渲染、交互和服务端接口统一在一个项目中 |
| 样式与组件 | Tailwind CSS + shadcn/ui | 快速构建一致、响应式的旅行规划界面 |
| 表单与校验 | React Hook Form + Zod | 旅行需求录入、服务端输入和模型输出校验 |
| AI | DeepSeek Chat Completions API | 需求结构化、候选行程生成、方案说明 |
| 地图与路线 | 高德地图 Web 服务 API + JS 地图 SDK | 地点搜索、地理编码、路线时间、地图展示 |
| 天气 | Open-Meteo（首期） | 国内城市天气预报、降水与温度风险 |
| 酒店 | Booking.com Demand API 或已确认准入的国内酒店供应商 | 可订酒店、价格、库存、详情与预订跳转 |
| 数据库 | PostgreSQL + Prisma ORM | 用户、行程、地点、实时查询快照与设置 |
| 缓存与限流 | Redis | 降低地图、天气、酒店 API 调用频率 |
| 部署 | 国内云平台 + 托管 PostgreSQL / Redis | 更稳定地服务国内用户，并便于调用国内服务 |

## 3. 系统架构

```text
浏览器（Next.js React 页面）
  │  输入、地图展示、行程编辑
  ▼
Next.js Route Handlers（服务端 BFF）
  ├── DeepSeek Provider：结构化需求、候选行程、文案生成
  ├── AMap Provider：地点、坐标、路线、距离与预计耗时
  ├── Weather Provider：按目的地与日期查询天气
  ├── Hotel Provider：酒店搜索、价格、库存、预订跳转
  ├── 行程优化模块：按交通时间重排每日地点
  ├── PostgreSQL：持久化行程、用户与查询快照
  └── Redis：缓存、限流、短时任务状态
```

Route Handlers 是业务数据 Provider 的唯一访问入口。前端只额外加载高德 JS 地图底图，并使用专用浏览器 Key；POI、路线、天气、酒店及 Tavily/AI 数据仍只经过内部 API。

## 4. 核心业务流程

```text
用户提交需求
  → DeepSeek 生成候选地点与活动类型
  → 高德检索和校验正式地点、地址与经纬度
  → 高德计算地点间交通时间矩阵
  → 优化模块重排当天访问顺序
  → 查询旅行日期对应的天气
  → 根据降雨、高温等情况替换或调整活动
  → 以每日行程重心查询酒店
  → 返回最终方案、地图路线、预算、天气与酒店卡片
```

模型只负责建议与解释；地点、时间、距离、天气、价格等事实数据必须以后端 API 查询结果为准。

## 5. 主要模块

### 5.1 旅行需求与 AI 规划

- 输入：目的地、日期、人数、预算、偏好、禁忌、出行方式、每日体力上限。
- DeepSeek 输出结构化候选行程，严格要求可检索的正式地点名称。
- 使用 Zod 校验模型输出；无正式地点名、地址或预计停留时间时，要求重试或标记为待补充。

### 5.2 地点与路线优化

- 用高德地点搜索将候选名称映射为标准名称、地址、经纬度和地点 ID。
- 用步行、公交、驾车/打车路径接口获取相邻地点的时间与距离。
- 先按“每天 4–8 个地点”优化，不引入独立算法服务。
- 用户锁定必去点、调整顺序或删除地点后，只重新计算受影响的一天。
- 输出每段的交通方式、预计时间、距离和查询时间；不承诺永久“最短”，只说明“按当前数据估算的较省时路线”。

### 5.3 天气与行程调整

- 按城市坐标和旅行日期获取日预报，展示气温、降水、风力和风险。
- 将地点标记为室内、室外或混合活动。
- 降雨、极端高温等情况触发“替代地点”或“与其他日期互换”的建议。
- 天气数据按城市和日期短期缓存，并显示查询时间。

### 5.4 酒店查询与跳转

- 输入：入住/退房、人数、房间数、预算、评分、位置偏好。
- 根据每日行程的地理重心推荐住宿区域，并在周边搜索酒店。
- 展示每晚价格、总价、评分、距离、早餐/取消政策摘要和供应商信息。
- 预订按钮只跳转至供应商深链；所有价格都带“查询时间”和“以预订页为准”提示。

## 6. 数据模型（后续引入数据库时）

| 实体 | 关键数据 |
| --- | --- |
| User | 基础资料、偏好设置、授权信息 |
| Trip | 目的地、日期、预算、人数、状态、最终方案 JSON |
| TripDay / Stop | 日期、地点 ID、顺序、活动类型、停留时间、用户是否锁定 |
| Place | 高德地点 ID、名称、地址、坐标、分类 |
| RouteSnapshot | 起终点、出行方式、距离、耗时、查询时间、原始数据摘要 |
| WeatherSnapshot | 坐标、日期、天气指标、预警、查询时间 |
| HotelSnapshot | 供应商、酒店 ID、查询条件、价格、库存、深链、查询时间 |

行程与供应商响应可存为 JSONB 快照，关键的日期、地点 ID、价格、状态等字段单独建列，方便查询和更新。

## 7. 接口分层原则

- 统一定义内部数据结构，例如 `Place`、`RouteLeg`、`WeatherDay`、`HotelOffer`。
- 每个供应商各自实现 Provider；业务逻辑只依赖内部结构，不依赖高德或酒店 API 的原始返回值。
- 高德作为首个且默认的地图 Provider。
- 酒店 Provider 在确认供应商准入前只保留接口定义和模拟数据，不在前端写死供应商格式。

## 8. 缓存、可靠性与安全

- 地点搜索可缓存 7–30 天；路线缓存按出行方式和起终点短期保存；天气和酒店价格缓存更短。
- 对所有第三方 API 设置超时、重试和降级提示。单个供应商失败不应导致整份行程不可查看。
- 环境变量保存 `DEEPSEEK_API_KEY`、`AMAP_API_KEY`、天气与酒店密钥；使用 `.env.local`，并加入 `.gitignore`。
- 记录供应商返回时间与数据来源，在页面展示“AI 建议”“实时查询”“待确认”三类状态。
- 对服务端接口做输入校验、限流与基础审计，避免 API Key 被滥用。

## 9. 实施顺序

### P0：项目基础迁移

- 将现有原型迁移到 Next.js + TypeScript。
- 建立 UI 组件、表单校验、服务端 API 分层和环境变量管理。
- 暂不引入数据库与用户体系。

### P1：可信路线 MVP（最高优先级）

- 接入高德地点搜索、地理编码、路径规划与地图展示。
- 校验 AI 输出地点，计算逐段时间，优化每天路线顺序。
- 支持锁定、删除、替换地点并重新计算。

### P2：天气驱动的行程调整

- 接入天气预报与风险卡片。
- 支持雨天/高温备选地点和一键调整建议。

### P3：酒店搜索与预订跳转

- 在确认供应商账号和商业条款后接入酒店查询。
- 增加住宿区域推荐、价格卡片和外部预订跳转。

### P4：账户与保存

- 接入 PostgreSQL、用户认证、行程保存、分享和历史版本。
- 加入 Redis 缓存与定时刷新能力。

## 10. 当前不做的事项

- 多国地图与海外地址体系。
- 站内支付、订单创建、退款、客服与供应商订单同步。
- 多人实时协作和复杂社区内容。
- 独立微服务、消息队列或大型路线优化集群。

这些能力在用户量、行程复杂度或供应商合作要求明确后再评估。

## 11. 餐饮与攻略实现

继续采用单体 Next.js，新增服务端 Food Provider 与可独立测试的餐饮约束/排序模块。编写 Route Handler 前阅读本地 Next.js route 与 environment-variables 指南。

### 数据流与 Provider

1. 校验预算口径、口味、忌口、最大绕路时间、用餐时长及用户粘贴的来源。
2. Tavily POST /search 限定 xiaohongshu.com，查询目的地、行程区域、口味、美食榜与攻略；返回标题、URL、摘要和检索时间。没有 TAVILY_API_KEY 时跳过自动搜索并明确告知。
3. DeepSeek 从不可信的来源文本抽取店名和 Tips，Zod 校验后逐条检查 sourceId 和逐字证据；拒绝无依据的输出。没有模型时使用与已知正式名称的文本匹配和原文片段降级。
4. 高德 v5/place/around 检索餐饮 POI，v5/place/text 查询来源中的店名并进行分店消歧；只有明确匹配的门店可关联证据。business.cost 生成保守人均区间，缺失不填虚构价格。营业时间只解析明确每日时段，其他格式保持待确认。
5. 高德 v3/direction/walking、driving、transit/integrated 查询路线，严格检查 status、数值及城市参数。每次生成内缓存相同路段，最多 4 个并发、启动间隔 400ms；QPS 临时拒绝最多重试 2 次，不重试无效密钥/日额度耗尽。请求超时/错误返回 pending，不能用直线距离假充实时路线。
6. 约束模块输出 MealSlot、RestaurantCandidate、RouteLeg、EvidenceTip、FoodSummary：先检查预算/绕路/用餐时间/忌口，再按偏好、来源证据与绕路排序，主选及两个备选互不重复。

### 换店与可信边界

- POST /api/plan 返回带随机 planId、revision 的方案；服务器进程内暂存有限数量、30 分钟过期，无持久化。
- POST /api/food 接受 planId、revision、mealId、action 与可选 restaurantId；仅使用服务器保存的候选，重新校验可行性并计算总费用和选中路线；旧版本返回冲突，锁定餐次不得被换店。
- 保存对象不含密钥，第三方异常不原样透传。所有 URL 只展示校验后的 HTTPS 链接，不由服务器任意抓取用户 URL。生成、换店接口限制请求体大小。
- 重启/多实例不共享内存会导致方案过期，前端提示重新生成；后续需要多实例时替换为持久化存储。
- 路线为查询时估算，不承诺未来交通；公交/打车费用采用公开路段费用或明确规则估算，缺失标为待确认。只计算新增餐饮交通，不将其误认为整趟交通已核验。

### 配置、验证与资料

- AMAP_API_KEY：具体门店、价格线索、营业时间与路线；TAVILY_API_KEY：公开小红书笔记检索；DEEPSEEK_API_KEY：来源提取。只读服务端环境变量或 .env.local，.env.example 仅空值。
- 单元测试覆盖证据绑定、恶意 URL、服务失败、成本区间、多人预算、路线差值、营业跨日、忌口、缺失数据、换店和锁定；HTTP 冒烟覆盖生成及换店接口。运行 npm run check 并执行浏览器验证。
- [Tavily Search 接口](https://docs.tavily.com/documentation/api-reference/endpoint/search)
- [高德地点搜索](https://lbs.amap.com/api/webservice/guide/api-advanced/newpoisearch)、[路径规划](https://lbs.amap.com/api/webservice/guide/api/direction)
- [小红书公开接口目录](https://openaccount.xiaohongshu.com/docs/api-reference)：本次未找到通用笔记搜索接口，因此不假设可直接调用小红书站内搜索。

## 12. 多日去重与餐饮检索修正（2026-09-07）

- `itinerary.mjs` 校验独立的 AI 候选数组（不再以 4 个 fixture 为模板截断），按天数确定候选数量；标准化名称和 POI ID 去重后进行一次地理排序、分段分配。模型信息不足则保留明确缺口，无循环复用。
- 去重后候选不足时通过高德目的地景点搜索补齐（类别 110000，最多两组搜索）；只接受景点类别和有效坐标，不把餐厅塞进景点日程。所有来源仍不足时才保留缺口。
- AI 新名称使用待核验坐标及费用标记，仅高德校验成功后参与餐厅检索。景点查询复用限流、超时和错误降级 Provider。
- 午餐 POI 查询覆盖前后站，候选缓存键同时包含前后坐标；每个区域取 20 项、去重并过滤非正餐分类。先按预算、营业及口味排序，首批计算 8 家路线；无可行解时再计算最多 8 家。其他餐次已选门店降低优先级。
- 正餐窗口最后一个景点安排在 16:30，为前一站保留至少 30 分钟交通间隔，同时避免模板固定 17:00 加游览时长挤掉整个晚餐窗口；仍执行用户时长、绕路和下一站时间约束。
- UI 主选缺失时直接呈现具体门店及 pending/reasons，保持服务端硬条件不变。新增 1/2/3/5/10 天去重测试、候选不足/别名/模型条数测试、餐厅后页和双端查询测试，以及三日生产接口与浏览器回归。

## 13. 多城市顺路规划与前端路线图（2026-09-07）

### 输入与校验

- `TripRequest` 使用 `origin` 与 `destinations[]`；目的地必须来自内置省市目录、全程去重、最多 10 个，且数量不能超过旅行天数。
- 省市目录是无密钥的本地静态数据，可安全进入客户端包；出发地保持自由文本，交由服务端高德地理编码校验。
- 表单通过内部 `POST /api/plan` 一次提交数组，Route Handler 继续使用 Zod 校验并限制请求体。

### 顺路算法与数据流

1. 服务端用高德地理编码获取出发地和各目的地城市中心坐标。
2. 以出发地为锚点运行最近邻启发式，逐次选择距离当前点最近且未访问的城市，减少明显折返；缺少地图配置时保持用户选择顺序。
3. 按城市顺序平均分配旅行天数，余数优先分配给前面的城市；每个目的地保证至少一天。
4. 每个城市独立执行 AI 候选、高德 POI 校验、景点补齐和内部地点排序。首个城市以出发地为排序锚点，后续城市以上一段末站为锚点。
5. 城市内餐厅搜索、路线增量和绕路上限使用 `Day.city` / `MealSlot.city`，避免多城市行程错误沿用首站城市参数。
6. 出发地到首城、城市到下一城市的衔接由高德服务端路线接口估算；步行优先只作用于市内，跨城默认查询公共交通，驾车优先则查询驾车。

最近邻是可解释、成本较低的 MVP 启发式，不声明全局最优。跨城路线与点位连线均显示高德来源、查询时间和待确认状态。

### 路线图边界

- 服务端返回统一的 `RouteOverview`：城市顺序、跨城 `RouteLeg`、查询状态，以及按时间顺序编号的 `RoutePoint`。
- `RoutePoint` 包含出发地、景点和已选餐厅；没有有效经纬度的待核验地点不伪装成地图点。
- React 客户端使用高德 JS API 2.0 渲染真实底图、服务端返回的道路折线与编号 Marker，提供全程/按日筛选和地点详情。JS Key 与安全密钥使用 `NEXT_PUBLIC_AMAP_JS_KEY` / `NEXT_PUBLIC_AMAP_SECURITY_JS_CODE`，必须配置域名白名单；业务 Web API Key 不进入客户端。
- 换店后 `PlanStore` 从服务端保存的合格候选重建餐厅点位和顺序编号，避免地图继续显示旧餐厅。

## 14. 候选发现、证据与导航（2026-09-07）

### 两阶段接口

1. `POST /api/discover` 使用完整 `TripRequest`，并行执行 Tavily 公开笔记搜索、DeepSeek 结构化候选/证据提取和高德地点核验，返回 `DiscoveryCandidate[]`、Provider 状态、警告、`discoveryId` 与过期时间。
2. `DiscoveryStore` 在服务端进程内保存标准化请求和候选 30 分钟，容量有上限并返回不可预测 UUID。`POST /api/plan` 只接受 `discoveryId + selectedIds[]`，检查 ID 归属、重复项、有效期及每城市至少一个景区。
3. `buildPlan` 直接采用已选择且已核验的景区，不再静默换回 AI 候选；已选餐厅转换为 `preferredRestaurants`，与周边高德候选一起执行原有硬约束和路线增量计算；娱乐地点在日间路线形成后另行筛选。

### 小红书证据链

- Tavily 请求使用 `include_domains: ["xiaohongshu.com"]`，仅保留经过 URL 白名单验证、包含非空公开摘要的结果并按标准路径去重。
- DeepSeek 只允许输出来源 ID、原文中逐字出现的具体店名、包含店名的连续引用和类别。服务端再次验证来源存在、引用可在摘要中定位、引用确实包含店名；验证失败的模型输出全部丢弃。
- 同名品牌不能自动继承其他分店证据。餐饮候选最终以高德 05 类 POI 为准；推荐排序结合独立来源数、近一年发布日期线索和用户餐饮偏好，但证据始终标为经验/榜单线索而非认证事实。

### 图片和高德导航

- 高德 POI 的 `photos` 只在服务端读取。前端图片 URL 指向 `GET /api/poi-image`；该代理仅允许无凭据、无自定义端口的高德/AutoNavi 子域名，强制 HTTPS、拒绝重定向、限制为图片 MIME 和 5 MB，避免任意 URL 抓取。
- 地点导航链接使用 `https://uri.amap.com/navigation`，包含已核验经纬度、地点名、GCJ-02 标记、交通模式和 `callnative=1`。坐标不在中国范围时不生成链接。
- 页面分别标注 AI、高德和公开搜索状态与查询时间；图片、地点、路线或证据不可用时均采用明确降级，不补造数据。

## 15. 路线 2.0、确认状态与逐日数据（2026-09-07）

- 高德路线 Provider 使用 `v5/direction/walking|driving|transit/integrated`，起终点存在 POI ID 时一并提交。统一 `RouteLeg` 增加 `polyline` 与可展示的 `error`，区分坐标未核验、无可用路线和服务超时/限流。
- `RouteOverview.paths` 保存相邻行程点的标准化道路折线；客户端只负责渲染，不直接计算路线。无 JS Key 时显示配置提示和地点顺序，不用示意直线伪装真实道路。
- `MealOption` 将 `eligible`（完整满足）、`canAcceptPending`（无硬冲突但字段未知）和 `reasons`（明确冲突）分开。用户已选餐厅优先占用未使用餐次；待确认值不会被填成 0，也不会被描述为已验证。
- Tavily Search 使用 `search_depth=advanced`、`chunks_per_source=3`，每个城市先执行三类发现查询，高德核验候选后再以具体分店名执行一次精准查询；仍限定 `xiaohongshu.com` 并进行 URL 白名单和证据逐字验证。AI 可返回菜名，但服务端只保留同一引用中真实出现的菜名。
- `Plan.dayGuides` 为每个日期返回城市天气、当天路线重心酒店建议和上下文提醒；天气与路线携带查询时间，酒店继续明确为非实时价格和库存。
- DeepSeek V4 默认启用高强度思考，短小的结构化候选/证据抽取请求显式使用 `thinking.type=disabled` 并限制输出长度，减少超时降级，同时继续用 JSON Schema/Zod 和逐字证据检查约束输出。

## 16. 自定义候选与当日重规划接口（2026-09-07）

- `POST /api/discover/custom` 接受 `discoveryId + city + kind + names[]`。服务端从 `DiscoveryStore` 读取原始请求，只允许当前目的地的景区/餐饮类别；高德返回结果还要与输入名称做标准化精确或双向包含匹配，避免把模糊搜索结果误当成用户指定地点。
- `DiscoveryStore.append` 只追加服务端核验后的候选并按服务端 ID 去重。客户端收到完整候选快照后自动勾选新增项，浏览器不能自行提交坐标、类别、图片或来源。
- `buildPlan` 先用景区骨架生成餐次，再按娱乐偏好查询高德 08 类 POI、计算末站到娱乐场所的路线并附加夜间活动，避免娱乐地点挤掉晚餐窗口。跨天自动主选按 POI 去重，交通超过 45 分钟或路线未知时不自动加入。
- `Plan.entertainmentDays[]` 保存每天的路线锚点、服务端签发的娱乐备选、主选 ID、交通时间/距离和降级提示。客户端下拉框只能提交已有选项 ID。
- `POST /api/plan/day` 使用 `planId + revision` 乐观并发控制，接收当天景区替换草稿和娱乐选项。`replanDay` 重新核验景区类别与名称、按前一日末站排序、重建餐次、酒店区域、路线点及道路折线；`PlanStore.replace` 仅在版本仍一致时提交新快照。
- `Stop.reservation` 使用可审计状态结构。`reservations.mjs` 每个城市只发起一次 Tavily Advanced 查询并限定 `.gov.cn` HTTPS 结果；摘要须包含准确景区名和明确预约措辞才设置 `required/recommended`，并保留来源链接与查询时间。其余情况默认 `unknown`；不得用模型输出直接设置预约状态。
